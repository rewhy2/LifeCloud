<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhixiang.ops.mapper.ScheduleMapper">

    <sql id="Base_Column_List">
        id, employee_id, employee_name, work_date, shift, create_time
    </sql>

    <select id="selectByDate" resultType="com.zhixiang.ops.entity.Schedule">
        SELECT <include refid="Base_Column_List"/> FROM schedule WHERE work_date = #{date} ORDER BY shift
    </select>

    <select id="selectRange" resultType="com.zhixiang.ops.entity.Schedule">
        SELECT <include refid="Base_Column_List"/> FROM schedule
        WHERE work_date BETWEEN #{start} AND #{end} ORDER BY work_date, shift
    </select>

    <insert id="insert" parameterType="com.zhixiang.ops.entity.Schedule" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO schedule (employee_id, employee_name, work_date, shift)
        VALUES (#{employeeId}, #{employeeName}, #{workDate}, #{shift})
    </insert>

    <delete id="deleteByDate">
        DELETE FROM schedule WHERE work_date = #{date}
        <if test="employeeId != null">AND employee_id = #{employeeId}</if>
    </delete>
</mapper>

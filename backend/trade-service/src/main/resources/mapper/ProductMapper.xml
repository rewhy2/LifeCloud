<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhixiang.trade.mapper.ProductMapper">

    <sql id="Base_Column_List">
        id, name, category_id, price, cost, status, description, image, sales_count, create_time
    </sql>

    <select id="selectAll" resultType="com.zhixiang.trade.entity.Product">
        SELECT <include refid="Base_Column_List"/>
        FROM product
        <where>
            <if test="name != null and name != ''">AND name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="categoryId != null">AND category_id = #{categoryId}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY id DESC
    </select>

    <select id="selectById" resultType="com.zhixiang.trade.entity.Product">
        SELECT <include refid="Base_Column_List"/> FROM product WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.zhixiang.trade.entity.Product" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (name, category_id, price, cost, status, description, image, sales_count)
        VALUES (#{name}, #{categoryId}, #{price}, #{cost}, #{status}, #{description}, #{image}, #{salesCount})
    </insert>

    <update id="update" parameterType="com.zhixiang.trade.entity.Product">
        UPDATE product
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="price != null">price = #{price},</if>
            <if test="cost != null">cost = #{cost},</if>
            <if test="status != null">status = #{status},</if>
            <if test="description != null">description = #{description},</if>
            <if test="image != null">image = #{image},</if>
            <if test="salesCount != null">sales_count = #{salesCount},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>
